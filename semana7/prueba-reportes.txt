Al enviar un reporte mediante la ruta POST, la información se guarda temporalmente en un arreglo llamado reportes. Cuando se consulta la ruta GET /reportes, el servidor devuelve la lista de todos los reportes registrados mientras el servidor permanece en ejecución. Si el servidor se reinicia, la lista vuelve a quedar vacía porque los datos solo se almacenan en memoria.

